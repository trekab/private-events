<h1>Log In</h1>

<%= form_for(:session, html: { role: "form"},  url: login_path) do |f| %>
  <div>
    <%= f.label :id, "User ID" %>
    <%= f.text_field :id, placeholder: "Enter User ID" %>
  </div>
  <%= f.submit "Log in" %>
<% end %>